<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HabitApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:textColorHint">@color/fifth</item>
    </style>

    <style name="Theme.HabitApp" parent="Base.Theme.HabitApp" />

    <style name="CustomDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Customize dialog background color -->
        <item name="android:background">@color/secondary</item>
        <item name="android:textColor">@color/primary</item>
    </style>
</resources>